function findPairDivisibleBy17() {
    for (let i = 1, i <= 100, i++) {
        console.log(i);
        for (let j = 1, j <= 100, j++) {
            const 

        }
    }
    const count =
    console.log(`\nTổng cộng: ${count} cặp`);
    )
}